# DocRAG 真实模型依赖（切换 RAG_*_MOCK=false 时才需要）。
# 镜像默认不安装此文件，以减小体积、加快构建；离线 MOCK 演示无需任何大模型权重。
#
# 安装方式（二选一）：
#   1) 容器内固定：  docker compose exec backend pip install -r requirements-ml.txt
#   2) 自制镜像：    在 Dockerfile 末尾追加 RUN pip install -r requirements-ml.txt 后重建
#
# 装上后将 docker-compose.yml / .env 中的 RAG_EMBED_MOCK / RAG_RERANK_MOCK /
# RAG_PARSE_MOCK / RAG_LLM_MOCK 改为 "false"，并挂载模型权重（volume docrag_models）。

# 解析（Docling，首次运行自动拉布局模型）
docling==2.117.0

# 嵌入 / 重排（BAAI/bge-m3、BAAI/bge-reranker-v2-m3）
flag-embedding>=1.3
sentence-transformers>=3.3
